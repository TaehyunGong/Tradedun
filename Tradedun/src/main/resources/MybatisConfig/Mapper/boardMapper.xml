<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.thkong.tradedun.Board.dao.boardDao">
	
	<select id='selectBoardNo' resultType="Int" parameterType="String">
		SELECT IFNULL(max(boardNo)+1,1) AS boardNo 
		FROM Board
		WHERE categoryCode = #{categoryCode}
	</select>
	
	<insert id="insertBoard" parameterType="Board">
		INSERT INTO Board (boardNo, categoryCode, title, contents, createDT, modifyDT, deleteDT)
		VALUES(#{boardNo}, #{categoryCode}, #{title}, #{contents}, NOW(), null, null)
	</insert>
</mapper>